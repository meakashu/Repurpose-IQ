# Optional ML dependencies (for local LLM inference)
# These may not be available for Python 3.13 yet
# Install only if you need local model inference

# transformers>=4.35.0
# sentence-transformers>=2.2.0
# torch>=2.6.0
# huggingface-hub>=0.19.0

# Note: The system works without these - it will use API-based LLMs instead

